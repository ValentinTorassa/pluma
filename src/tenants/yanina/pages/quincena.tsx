import type { TenantPages } from "../../types";

/** Yanina no publica newsletter (feature `quincena` apagada): las rutas responden 404. */
export const quincenaPage: TenantPages["quincena"] = null;
export const issuePage: TenantPages["issue"] = null;
