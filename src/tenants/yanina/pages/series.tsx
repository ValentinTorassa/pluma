import type { TenantPages } from "../../types";

/** Yanina no tiene series (feature `series` apagada): las rutas responden 404. */
export const seriesIndexPage: TenantPages["seriesIndex"] = null;
export const seriesPage: TenantPages["series"] = null;
