import type { TenantPages } from "../../types";

/** Yanina no publica newsletter (feature `apuntes` apagada): las rutas responden 404. */
export const apuntesPage: TenantPages["apuntes"] = null;
export const issuePage: TenantPages["issue"] = null;
